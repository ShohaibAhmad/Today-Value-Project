<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.promoteprovider.todayvalue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodayValue"
        android:usesCleartextTraffic="true">
        <activity android:name="com.promoteprovider.todayvalue.ResultActivity"></activity>
        <activity android:name="com.promoteprovider.todayvalue.QuizActivity" />
        <activity android:name="com.promoteprovider.todayvalue.SignUpActivity">

        </activity>
        <activity android:name="com.promoteprovider.todayvalue.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.promoteprovider.todayvalue.MainActivity">

        </activity>
    </application>

</manifest>